# project 能力

状态：Baseline；目标 owner：project；最低实施等级：L2；复核/批准等级：L3。负责项目、仓库、目标 ref、workspace 和项目配置。当前代码主要位于 `gate-web` 路由及 JDBC repository，迁移目标是 project use case/port。数据见 ownership catalog；API 为 `/api/projects/**`、`/api/workspaces/**`；遗留拆分进入 DEBT-001/009。

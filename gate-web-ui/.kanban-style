<style scoped>
.kanban-page {
  display: flex;
  flex: 1;
  flex-direction: column;
  gap: 20px;
  min-height: 0;
  padding: clamp(24px, 3.5vw, 48px) clamp(20px, 4vw, 56px) 32px;
  overflow: auto;
}
.kanban-intro {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 28px;
  max-width: 1500px;
}
.kanban-intro__copy {
  max-width: 760px;
}
.kanban-eyebrow {
  margin: 0 0 11px;
  color: var(--accent, #e66d2f);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.17em;
}
.kanban-intro h2 {
  margin: 0;
  color: var(--text, #172536);
  font-size: clamp(27px, 3vw, 42px);
  font-weight: 760;
  letter-spacing: -0.05em;
  line-height: 1.08;
}
.kanban-intro__lede {
  max-width: 610px;
  margin: 13px 0 0;
  color: var(--text-muted, #718092);
  font-size: 13px;
  line-height: 1.75;
}
.kanban-intro__metric {
  display: flex;
  flex: none;
  flex-direction: column;
  align-items: flex-end;
  padding: 2px 0 3px 26px;
  border-left: 1px solid var(--border-strong, #cbbfaf);
}
.kanban-intro__metric strong {
  color: var(--text, #172536);
  font-size: 36px;
  font-weight: 780;
  letter-spacing: -0.06em;
  line-height: 1;
}
.kanban-intro__metric span {
  margin-top: 7px;
  color: var(--text-muted, #718092);
  font-size: 9px;
  font-weight: 800;
  letter-spacing: 0.14em;
}
.kanban-toolbar {
  position: sticky;
  top: -1px;
  z-index: 4;
  display: flex;
  align-items: center;
  gap: 11px;
  max-width: 1500px;
  min-height: 44px;
  padding: 8px 0;
  background: linear-gradient(var(--background, #f3efe8) 75%, transparent);
}
.view-toggle {
  display: inline-flex;
  flex: none;
  gap: 2px;
  padding: 3px;
  border: 1px solid var(--border, #e4ddd3);
  border-radius: 10px;
  background: var(--panel, #fffdfa);
}
.view-toggle button {
  min-width: 54px;
  min-height: 29px;
  padding: 0 10px;
  border: 0;
  border-radius: 7px;
  color: var(--text-muted, #718092);
  background: transparent;
  font-size: 12px;
  font-weight: 700;
  cursor: pointer;
  transition: color 0.16s ease, background 0.16s ease, box-shadow 0.16s ease;
}
.view-toggle button:hover {
  color: var(--text, #172536);
}
.view-toggle button.active {
  color: var(--text, #172536);
  background: var(--panel-2, #f7f2ea);
  box-shadow: 0 1px 4px rgba(30, 38, 50, 0.1);
}
.toolbar-search {
  position: relative;
  display: flex;
  align-items: center;
  gap: 7px;
  width: min(300px, 34vw);
  min-height: 35px;
  padding: 0 10px;
  border: 1px solid var(--border, #e4ddd3);
  border-radius: 9px;
  color: var(--text-muted, #718092);
  background: var(--panel, #fffdfa);
  transition: border-color 0.16s ease, box-shadow 0.16s ease;
}
.toolbar-search:focus-within {
  border-color: var(--accent, #e66d2f);
  box-shadow: 0 0 0 3px var(--accent-soft, rgba(230, 109, 47, 0.14));
}
.toolbar-search :deep(.g-input) {
  min-height: 33px;
  padding: 5px 26px 5px 0;
  border: 0;
  color: var(--text, #172536);
  background: transparent;
  box-shadow: none;
}
.toolbar-search :deep(.g-input:focus) {
  border: 0;
  box-shadow: none;
}
.toolbar-search__clear {
  position: absolute;
  right: 8px;
  display: grid;
  width: 20px;
  height: 20px;
  place-items: center;
  border: 0;
  border-radius: 50%;
  color: var(--text-muted, #718092);
  background: var(--panel-2, #f7f2ea);
  cursor: pointer;
}
.toolbar-count {
  color: var(--text-muted, #718092);
  font-size: 11px;
  white-space: nowrap;
}
.spacer {
  flex: 1;
}
.kanban {
  display: grid;
  flex: 1;
  grid-template-columns: repeat(8, minmax(226px, 1fr));
  gap: 12px;
  align-items: stretch;
  min-height: 420px;
  max-width: 1500px;
  padding-bottom: 6px;
  overflow-x: auto;
  overflow-y: hidden;
}
.kanban-col {
  display: flex;
  flex-direction: column;
  min-width: 226px;
  min-height: 0;
  overflow: hidden;
  border: 1px solid var(--border, #e4ddd3);
  border-radius: 14px;
  background: rgba(255, 253, 249, 0.75);
  box-shadow: 0 8px 20px rgba(40, 51, 66, 0.035);
}
.kanban-col__head {
  display: flex;
  flex: none;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  min-height: 51px;
  padding: 0 13px;
  border-bottom: 1px solid var(--border, #e4ddd3);
  background: rgba(255, 255, 255, 0.38);
}
.kanban-col__title {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  color: var(--text-secondary, #405064);
  font-size: 12px;
  font-weight: 720;
}
.stage-mark {
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: #a8afb8;
  box-shadow: 0 0 0 3px rgba(168, 175, 184, 0.14);
}
.kanban-col[data-stage="IN_PROGRESS"] .stage-mark,
.kanban-col[data-stage="PRESUBMITTED"] .stage-mark,
.kanban-col[data-stage="IN_REVIEW"] .stage-mark {
  background: var(--accent, #e66d2f);
  box-shadow: 0 0 0 3px var(--accent-soft, rgba(230, 109, 47, 0.14));
}
.kanban-col[data-stage="READY_TO_PUBLISH"] .stage-mark,
.kanban-col[data-stage="DONE"] .stage-mark {
  background: var(--success, #2c8962);
  box-shadow: 0 0 0 3px rgba(44, 137, 98, 0.14);
}
.kanban-col[data-stage="NEEDS_HUMAN"] .stage-mark {
  background: var(--warning, #b87921);
  box-shadow: 0 0 0 3px rgba(184, 121, 33, 0.14);
}
.kanban-col[data-stage="CANCELLED"] .stage-mark {
  background: var(--danger, #c2534a);
  box-shadow: 0 0 0 3px rgba(194, 83, 74, 0.14);
}
.kanban-col__count {
  display: inline-grid;
  min-width: 23px;
  min-height: 22px;
  place-items: center;
  padding: 0 6px;
  border: 1px solid var(--border, #e4ddd3);
  border-radius: 999px;
  color: var(--text-muted, #718092);
  background: var(--panel-2, #f7f2ea);
  font-size: 10px;
  font-weight: 750;
}
.kanban-col__cards {
  display: flex;
  flex: 1;
  flex-direction: column;
  gap: 9px;
  min-height: 0;
  padding: 10px;
  overflow-y: auto;
}
.issue-card {
  padding: 13px 13px 12px;
  border: 1px solid var(--border, #e4ddd3);
  border-radius: 11px;
  color: var(--text, #172536);
  background: var(--panel, #fffdfa);
  cursor: pointer;
  box-shadow: 0 4px 10px rgba(40, 51, 66, 0.045);
  transition: border-color 0.16s ease, box-shadow 0.16s ease, transform 0.16s ease;
}
.issue-card:hover,
.issue-card:focus-visible {
  border-color: rgba(230, 109, 47, 0.46);
  outline: none;
  box-shadow: 0 9px 20px rgba(40, 51, 66, 0.1);
  transform: translateY(-2px);
}
.issue-card--rejected {
  border-color: rgba(194, 83, 74, 0.44);
  background: linear-gradient(180deg, rgba(194, 83, 74, 0.08), transparent 65%), var(--panel, #fffdfa);
}
.issue-card__top {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 8px;
}
.issue-card__key {
  color: var(--accent, #e66d2f);
  font-family: var(--font-mono);
  font-size: 10px;
  font-weight: 760;
}
.issue-card__top-right {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 5px;
  min-width: 0;
}
.issue-card__priority {
  padding: 2px 6px;
  border-radius: 999px;
  color: var(--text-muted, #718092);
  background: var(--panel-2, #f7f2ea);
  font-size: 9px;
  font-weight: 800;
}
.issue-card__priority--p0 {
  color: #b54c4c;
  background: rgba(194, 83, 74, 0.13);
}
.issue-card__priority--p1 {
  color: #966b34;
  background: rgba(184, 121, 33, 0.14);
}
.issue-card__title {
  display: -webkit-box;
  margin: 9px 0 2px;
  overflow: hidden;
  color: var(--text, #172536);
  font-size: 13px;
  font-weight: 680;
  line-height: 1.43;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}
.issue-card__project {
  display: flex;
  align-items: center;
  gap: 8px;
  margin: 9px 0 7px;
  color: var(--text-muted, #718092);
  font-size: 10px;
}
.issue-card__project-link {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  min-width: 0;
  overflow: hidden;
  color: var(--text-secondary, #405064);
  text-overflow: ellipsis;
  white-space: nowrap;
}
.issue-card__deps {
  display: inline-flex;
  align-items: center;
  gap: 3px;
  color: var(--text-faint, #9da5ad);
}
.issue-card__labels {
  display: flex;
  flex-wrap: wrap;
  gap: 4px;
  margin-bottom: 12px;
}
.issue-card__label {
  padding: 2px 7px;
  border: 1px solid rgba(230, 109, 47, 0.12);
  border-radius: 5px;
  color: var(--accent, #e66d2f);
  background: var(--accent-soft, rgba(230, 109, 47, 0.14));
  font-size: 9px;
}
.issue-card__label-more {
  align-self: center;
  color: var(--text-faint, #9da5ad);
  font-size: 10px;
}
.issue-card__bottom {
  display: flex;
  align-items: center;
  gap: 6px;
  padding-top: 9px;
  border-top: 1px solid rgba(228, 221, 211, 0.72);
  color: var(--text-muted, #718092);
  font-size: 10px;
}
.issue-card__assignee {
  max-width: 82px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.issue-card__round {
  padding: 2px 6px;
  border-radius: 999px;
  color: #a45d55;
  background: rgba(194, 83, 74, 0.11);
  font-size: 9px;
  font-weight: 800;
}
.issue-card__comment {
  display: inline-flex;
  align-items: center;
  gap: 3px;
  color: var(--text-faint, #9da5ad);
}
.issue-card__tokens {
  color: var(--text-faint, #9da5ad);
  font-size: 9px;
}
.kanban-col__empty {
  padding: 24px 9px;
  border: 1px dashed var(--border-strong, #cbbfaf);
  border-radius: 9px;
  color: var(--text-faint, #9da5ad);
  font-size: 11px;
  text-align: center;
}
.list-panel {
  flex: 1;
  min-height: 420px;
  max-width: 1500px;
  overflow: auto;
  border: 1px solid var(--border, #e4ddd3);
  border-radius: 14px;
  background: var(--panel, #fffdfa);
  box-shadow: 0 8px 22px rgba(40, 51, 66, 0.05);
}
.list-panel__head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 16px 18px;
  border-bottom: 1px solid var(--border, #e4ddd3);
}
.list-panel__head > div {
  display: flex;
  align-items: baseline;
  gap: 10px;
}
.list-panel__title {
  color: var(--text, #172536);
  font-size: 14px;
  font-weight: 740;
}
.list-panel__sub,
.list-panel__count {
  color: var(--text-muted, #718092);
  font-size: 11px;
}
.g-table {
  width: 100%;
  border-collapse: collapse;
  font-size: 12px;
}
.g-table th {
  position: sticky;
  top: 0;
  z-index: 1;
  padding: 10px 16px;
  border-bottom: 1px solid var(--border, #e4ddd3);
  color: var(--text-muted, #718092);
  background: var(--panel-2, #f7f2ea);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.04em;
  text-align: left;
}
.g-table td {
  padding: 13px 16px;
  border-bottom: 1px solid rgba(228, 221, 211, 0.72);
  color: var(--text-secondary, #405064);
  cursor: pointer;
}
.g-table tbody tr:last-child td {
  border-bottom: 0;
}
.g-table tbody tr:hover td {
  color: var(--text, #172536);
  background: rgba(242, 233, 223, 0.6);
}
.g-table td:first-child {
  color: var(--accent, #e66d2f);
  font-size: 11px;
  font-weight: 700;
}
.g-table__empty {
  padding: 44px 16px !important;
  color: var(--text-faint, #9da5ad) !important;
  text-align: center;
  cursor: default !important;
}
.mono {
  font-family: var(--font-mono);
}
.form {
  display: grid;
  gap: 15
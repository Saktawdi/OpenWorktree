# 更新日志

> 维护约定：新版本条目加在最上方；每个版本一个小节，标题以版本号开头（如 `## 0.2.0`，可带日期等后缀）；正式发行时去掉「（未发行）」标注。应用内按版本号截取对应小节展示。
>
> 版本号规则（x.y.z）：每次改动自行判断更新类型——大更新 y+1，bug 修复/优化等小更新 z+1；同一未发行小节内多条改动逐次累加（距上次升版提交的每个改动各进一格）。

## 0.3.8（未发行）

### 修复

- **点击会话 item 与运行监控跳转语义对齐**：会话有待回答/待授权时，从运行监控点击条目会清掉顶栏 chip 黄组与工单徽标并恢复待决卡片，但单独点击会话列表里的该会话两者都不发生——不清提醒（chip/徽标/会话黄点常驻，与监控跳转后的状态割裂），也不补拉待决卡片，且历史重建会整表替换聊天视图把已挂的提问/权限卡片冲掉（表现为「点了会话却不弹回答/授权窗口」）；现 `switchSession`（live）与「前往处理」同语义：点开即视为已关注该会话——待决登记与中断标记一并清除（忽略表继续兜住重拉），并在历史重建后再补拉未决的权限/提问卡片（归档会话跳过），卡片先挂后冲的顺序问题一并消除
- **专注编辑描述框溢出到下方区域的布局 bug**：专注编辑模式下描述编辑区的收缩下限此前压在 textarea 自身（硬性 320px），弹窗可用高度不足时外层描述区先被压缩、textarea 却不跟随收缩，描述框便溢出描述区边界、压到下方备注/标签之上（表现为「描述的框过大、跑到描述区下面去」）；现把高度下限上移到描述区容器层（240px）、textarea 改为随容器自由伸缩，空间不足时整块压缩并由外层滚动接管，不再越界覆盖

### 优化

- **新建工单描述框同步加高**：新建工单表单的描述编辑框此前仍是 4 行小框，与编辑弹窗普通模式加高后的体验不一致；现同步加高至 8 行（h-16 → h-40），长描述录入不再憋屈
- **桌面 dev 垫片收敛到 sidecar-jvm-shim（文档与开发工具整理）**：移除被取代的 `desktop/dev-shim-ow`（含已入库的 `ow-x86_64-pc-windows-msvc.exe.devshim-backup` 残留），`desktop/README.md` 本地 dev 节改写为现行垫片的用法（仓库根逐级探测 / `.sidecar-classpath` junction 伺服 SPA / kill-on-close 防孤儿），备份后缀统一为 `.shim-backup`；主 README 增补「插件生态」章节（SDK 契约 / 模板 / quick-quotes / deploy 探活安装，并直链各 README）与快速开始「方式二：Windows 桌面版」；plugin-sdk README 同步到现行契约（六插槽：+header.actions / floating.widgets；四权限：+storage / llm；同代次增量语义）

## 0.3.7（未发行）

### 新增

- **编辑工单描述支持专注编辑**：编辑弹窗的描述框此前仅 3 行高且不可调，长文本编辑体验很差；现标题行新增「专注编辑」按钮——弹窗放大为近全屏面板、描述区占据主体（等宽字体、实时字数），标题/优先级/备注/标签折叠收纳，Esc 或点右上角按钮退回普通弹窗；普通模式描述框同步加高（h-20 → h-40），改动统一仍由底部「保存修改」提交
- **看板卡片展示工单轮次徽标**：卡片底部此前 agent 徽标移除后左侧留空，现改为「第 N 轮」chip——轮次与已锁定快照的审查轮对齐（每次预提审进入新一轮，重启工单轮次亦加一），后端 `/api/tickets` 工单 JSON 新增 `review_round` 字段（快照最大轮与状态变更行最大轮取大者，未提审按第 1 轮计），看板/拖拽浮层同步呈现
- **设置中心改版：新增「偏好设置」与「存储设置」页，「应用设置」更名「关于」**：设置导航调整为偏好设置 → 存储设置 → 系统设置 → MCP 状态 → LLM 设置 → 关于，进入设置默认落在偏好设置。偏好设置首位放**输入排队**——原输入框工具行的「输入：排队/插队」开关迁入此处，改为两张语义更明确的选择卡片（排队 = Agent 工作时回车入队、空闲自动发送；插队 = 回车直接注入当前回合，仅 opencode），底层仍读写同一 `followUpBehavior` 偏好，Composer 行为不变；同页预留**语言**切换入口（简体中文 / English，标注「即将支持」暂不可点）。存储设置为预留占位页（数据目录查看、缓存清理、本地数据管理规划中）。输入框工具行不再提供排队快捷开关
- **运行监控 Chip 升级为会话维度与氛围提示（T-110）**：运行状态指示细化到具体会话，增加待回答与中断状态的视觉氛围提示
- **审查结果展示优化**：审查结果不再强制跳转/收叠会话列表，工单列表增加「已审查/驳回」结果徽标

### 修复

- **运行监控提醒「点击即处理」不再常驻红/黄（T-110）**：chip 的【待回答/待授权】与【中断事件】分组条目此前点击只跳转、不移除，导致提醒一直亮着；现点击跳转即移出监控——待决 id 记入忽略表（live 打开工单/慢节拍的后端重拉不再把它重新点亮，作答后忽略解除、agent 发起新 id 的待决会再次提醒），中断条目同时清除会话中断标记；监控同步新增【审查结果】分组，与「已结束」同级绿色低优呈现（含通过/需人工/驳回，驳回行红色区分），点击打开工单即视为已读移出
- **运行监控红/黄条目点击后仍残留（T-110 复审修复）**：点击【待回答/待授权】条目只清待决登记、点击【中断事件】条目只清中断标记——同一会话若两种状态并存，点击任何一组后另一组的对应条目仍驻留，红/黄氛围看起来"点不掉"；现统一为「点击 = 已关注该会话」：点击任一条目即把该会话的待决登记与中断标记一并移出监控（忽略表继续兜住打开工单/慢节拍的重拉，作答后解除）。同时修掉处理链路断点——点击跳转此前只做本地选中、不拉任何数据：会话列表/历史/待决卡片从未加载过（刷新后提醒重亮即点、慢节拍补拉来的提醒）时落地是空列表空对话，条目无从处理，提醒被清、服务端 ask 却仍挂起，agent 干等且重拉后又亮黄；现红/黄条目点击在 live 下改走工单完整打开路径（会话列表/历史/待决权限与提问卡片/门禁状态一次拉齐），会话内就地作答才是服务端真正的了结，提醒自此不再复发
- **运行监控跳转未聚焦目标会话（T-110 复审修复）**：live 模式点击红/黄条目走工单完整打开路径时未传目标会话，多会话工单下视图停留在默认会话，无法就地处理产生待办的会话；现 `selectTicketLive` 支持传入会话 id，点击条目即打开工单并聚焦目标会话（消息历史/待决卡片随之就位）
- **流式思考独白防串流**：`message.part.delta` 事件分类锚点修正为 `part.type`（因 reasoning 正文字段也为 text），防止思考内容串入正文流
- **opencode 原生逐 token 事件接入**：直接消费 opencode `message.part.delta` 事件，流式输出真正实时
- **工单终态流转与看板状态优化**：终态工单出口口径收敛；看板中「进行中」工单支持退回「待处理」

## 0.3.6（未发行）

### 新增

- **Agent 输出时支持用户消息排队与插队（T-107，参考 OpenChamber followUpBehavior）**：Agent 正在工作时输入框不再被锁死——回车把消息送入**按会话隔离的排队队列**（输入卡上方出现排队面板：排队数、上移/下移调整顺序、编辑取回输入框、删除、闪电按钮立即发送），会话一空闲即按 FIFO 自动逐条发送，刷新页面后队列仍在（localStorage 按会话 id 持久化）；跟随行为可在输入卡一键切换「排队 / 插队」（默认排队，持久化）：插队模式下回车把消息以 `delivery=steer` 实时注入 opencode 运行中的回合，`Ctrl+Enter` 走另一条（排队模式 `Ctrl+Enter` 即插队）；Claude 等不支持实时插队的 Agent 收到插队意图时提示并自动降级入队。后端 `POST /sessions/{id}/messages` 新增可选 `delivery` 字段透传给 opencode `prompt_async`（消息体加 `delivery` 段），Claude 适配器忽略该字段维持串行语义；插队不改变回合生命周期——被注入的回合仍只产生一次 idle 终点，busy 计数与上游回合缓冲归原回合所有（无在跑回合时后端把 steer 降级为普通新回合并补计），riding 判定前抛错（会话续接信息缺失、serve 拉起失败等）同样按骑乘失败口径处理——catch 只释放本任务真正持有的计数，绝不误清在跑回合的 busy、也不向正在流的回合补发错误帧，前端复用现有 SSE 事件流渲染续写、不另开流也不提前释放 busy；删除会话时先清排队队列再释放 busy（store 订阅同步触发，避免残留队列被自动泵投递给已删除会话），列表收敛/带外删除路径同样清理队列
- **插队消息按注入位置渲染（T-107 渲染修复，对齐 OpenChamber）**：插队消息的显示位置此前两处都不对——历史视图里 steer 的 USER 行以发送时刻（回合中段）落库、被插队的整条回复却到 idle 才落库，按时间排序插队行必然排到整条回复之前（表现为「紧跟第一条消息」）；live 视图里插队气泡吊在流式回合条目尾部、被不断增高的回复一路顶下去，且「数组末项是否流式」的滚动判定随之失效引发抖动。修复后回合内位置由时间线承载、不再依赖行间排序：后端 `TurnPart` 新增 `steer` 分段（含 user 行 id + 文本），reader 在回合中段收到 user 公告时把分段注入 `turnParts` 并补发 `steer_injected` SSE 事件（按 messageId 去重——同一消息的创建/完成两帧只触发一次注入，TTL 过期/回合终点/superseded 重置三路清理待注入条目，公告缺失绝不串位）；`POST /messages` 支持可选 `client_message_id` 贯通——USER 行以该 id 落库，乐观气泡与落库行同 id，历史重载按 steer 段携带的行 id 去重被回合吞并的顶层 USER 行、原位渲染；前端插队气泡改插入流式回合的时间线（受理即迁入，`steer_injected` 帧按 id 幂等对账兜底，帧携带的权威文本含后端追加的图片引用行时会写回既有分段——插队图片的缩略图在流式期间即随引用行还原，无需整页刷新），续写正文接在其后自然成段，流式判定改按条目语义扫描（任一 assistant 流式即吸附底部）。同时修复两处顺带缺陷：时间线 parts 落库读回时 `thinking` 段被降级成纯文本（历史重载思考折叠块丢失）；ChatRail 用户消息过滤无类型谓词导致的 TS 编译错误（`tsc -p` 全量失败）
- **看板「进行中」可拖回「待处理」**：工单开始后发现还没到动手时机时，可把卡片拖回待处理重新排队——纯队列内流转，无需理由、不动门禁，后端 PATCH 同口径放行（IN_PROGRESS → PENDING）；其余门禁态流转仍由预提审/审查/发布驱动不变

### 优化

- **回复 footer 标注升级为逐消息精确模型（V22）**：AI 气泡底部的模型标注从「会话当前模型的近似回退」升级为逐消息精确——`session_message` 新增 `model_provider`/`model_id` 两列（V22 迁移，存量行 NULL），落库口径为上游实际值优先（opencode `message.updated`/断线回填的 info 字段、claude stream-json 的 `message.model`/`result.model`），上游未上报时回退发送端请求值（opencode prompt body 的 model、claude `--model`）；历史回放经 `/messages` 响应的 `model_id` 逐行还原，旧行回退既有近似标注；live 流式气泡在占位时即按发送端同口径盖戳，流式/收尾/历史三路同值。切换模型只在下一回合生效的既有语义下，回合内模型恒定，一回合一条落库行恰好对齐逐消息粒度
- **推理强度标注同步逐消息落库（V23）**：footer 的推理强度档位与模型标注同口径升级——`session_message` 新增 `reasoning_variant` 列（V23 迁移），落库值为发送端钉住的请求档位（上游 CLI 不回传实际 effort：opencode 取 prompt body 的 variant、claude 取 `--effort`），历史回放经 `/messages` 响应的 `reasoning_variant` 逐行还原，未选档位/存量行为 NULL 回退既有近似标注；前端流式占位与历史重放两路同值
- **回复 footer 标注改显示当时请求的模型名**：AI 气泡底部此前显示 Agent 配置名称（如「默认智能体」），现在与 openchamber/docx 同式显示当时请求的模型名（等宽字体）——取值口径与发送端一致（会话实时覆盖 → 会话持久覆盖 → Agent 默认模型的 model 段），全部解析不出才回退 Agent 名称；流式回合收尾即盖戳，历史重载由 defaults 兜底补齐，两路径同口径
- **审查结果不再打断当前视图**：AI/人工审查判决落盘后不再强制切到「审查发现」页签、进入预提审阶段也不再自动收叠右侧面板的会话列表——结果改由右侧面板判决卡、会话流结论消息与工单列表徽标三路呈现，用户停在哪个视图就留在哪个视图；审查任务失败的强制跳转保留（错误必须被看见）
- **工作台顶栏去掉模型后缀**：工单标题行的 Agent 徽标不再拼「 · 模型名」长串（与图一 footer 重复且挤压标题栏），只留 Agent 名称，模型信息由会话气泡 footer 与输入框模型选择器承载

### 修复

- **已取消工单拖到「已完成」弹窗确认后才报错**：看板此前只拦「已完成」卡片的拖拽，已取消卡片拖到任意甬道都会弹出强制流转确认框，填完理由提交才被后端 400 打回（报错还是误导性的 "review-gated stage" 文案）——现前后端同口径「终态不出门」：终态工单唯一出口是右侧面板「重启工单」（带理由转回进行中），看板拖拽直接弹回并指向重启入口，后端报错文案同步改准确；后端同时把 PATCH 的终态互转（如 已取消 → 已完成）显式拒绝，堵住 API 直调的假可用路径

- **粘贴图片不再把 [图片 #n] 引用文本塞进输入框**：粘贴/拖入图片只在上方的 chip 胶囊占位（与引用胶囊同款，缩略图+文件名+移除钮），输入框正文保持用户原文；[图片 #n] 引用行改在发送时统一追加在消息尾部（永远独占一行），Agent 可见的引用语义不变；顺带修掉「移除附件 chip 后草稿残留引用文字」
- **图片引用与正文同行时正文被渲染吞掉**：用户气泡的引用清理此前按整行丢弃——行首命中 [图片 #n] 的整行（连同用户正文）一并消失，表现为「只发了图没有文字」（正文其实已送达 Agent）；现改为 token 级剥离（标记+紧随路径 token，CJK 正文不误吞），实时气泡、消息刻度悬浮预览与历史回放（mapHistoryMessage）三个视图同一口径，重切入会话不再出现漏出引用痕迹的另一种渲染
- **图片缩略图加载失败给出确定态**：工作区缩略图丢失/端点 404 时气泡显示「图片已失效」占位，不再永远脉冲的加载骨架

## 0.3.5（未发行）

### 修复

- **任务清单生命周期重构（串会话/刷新丢失/删除残留/清空不一致）**：任务清单此前按工单号键控且只内嵌在消息历史的 todowrite 参数里——同工单多会话互相覆盖显示、页面刷新后只有手动打开过的会话能重建、删除会话不清残留、`todowrite` 写空数组时流式与历史两条路径行为相反。重构为后端一等实体：新表 `session_todo`（V21 迁移，按会话 id 键控，删除会话 FK 级联清理），两个 CLI 适配器在 tool 事件到达时即时落快照（不等整回合 idle）；`GET /sessions/{id}/messages` 响应附带 `todos`（切会话一次请求同时拿到清单，流式中切走再切回也正确）、新增轻端点 `GET /sessions/{id}/todos`（回合结束轮询兜底，不再全量拉历史重扫）、存量会话首次读取时 lazy 回填；前端 store 投影改按会话 id 键控并收敛到 todos.ts 单一属主模块（SSE write-through + messages 附带查询 + 兜底三条对齐路径），侧栏任务环读当前活跃会话、草稿态天然无清单；「空数组=显式清空」前后端统一语义

## 0.3.4（未发行）

### 优化

- **接入 opencode 原生逐 token 事件（message.part.delta），live 流式真正实时**：此前只消费节流快照 `part.updated`（opencode 内部按秒级合并的广播），思考/正文整块才出现、长思考"卡半天"；实际 opencode 原生提供 ~60 次/秒的真增量事件（openchamber 同款消费方式，此前一直漏接）。现 delta 驱动实时流（field=reasoning→思考、text→正文），快照降级为对齐基准：两路共用累计正文对齐 + 最长后缀重叠剔除（openchamber event-reducer 同款语义），绝不重复/丢失；持久化仍以快照为准。前端零改动（时间线本就按 delta 累积设计），思考条预览与正文从此打字机式逐字刷新
- **思考条实时输出预览**：时间线思考行折叠态新增思考正文尾部预览（与其他工具行的参数预览同布局）——取正文尾部窗口压成单行，随流式增量实时滑动刷新，流式中呈亮色、结束后转灰，用户看得到"模型正在输出"而不是怀疑卡死；点击仍展开全文，展开态隐藏预览

### 修复

- **回合时间线 live 视图正文段蒸发**：token 事件只维护了兼容字段 `text` 与思考段封口，从未把正文增量写进 `parts` 时间线——流式期间所有正文段（含最终汇报）不可见，重切入会话才从落库历史回来（时间线渲染以 `parts` 为事实来源）。现正文 token 增量接入文本段（末位续写/否则新开），流式光标随段尾渲染

## 0.3.3（未发行）

### 新增

- **粘贴/拖入非图片文件自动落盘为工作区路径**：对话输入框粘贴非图片文件（或从资源管理器拖入）不再只有「浏览器拿不到路径」的占位提示——文件内容上传落盘到工单克隆 `.gate/chat-files/`（带鉴权新端点 `POST /api/tickets/{no}/chat-files`，50MB 上限、文件名白名单化、与 chat-images 共用 `.git/info/exclude` 收敛），发送成功后把落盘相对路径自动插进光标处，Agent（cwd 即克隆根）可直接读取；剪贴板文本里带「复制文件地址」的绝对路径时沿用直插路径；拖拽文件进输入框从此真正可用（此前拖入会整页导航走），与粘贴共用同一条图片/文件路由；demo 模式无后端时维持占位提示

### 修复

- **桌面端粘贴图片+文字发送必 500、消息全局丢失（T-112）**：桌面版后端是 GraalVM native-image 二进制，不携带 awt 本地库，`javax.imageio.ImageIO` 类初始化直接失败（`UnsatisfiedLinkError: no awt` → 后续 `NoClassDefFoundError`）——抛出的是 Error，逃过了缩放/落盘两层 `catch (Exception)` 与 Javalin 的 Exception 处理器，直接 500；缩略图保存又发生在用户消息落库之前，整条发送在落盘前中止。修复：发送路径彻底移除 ImageIO 依赖（图片原样落盘，最长边的预缩放移到前端 canvas，`catch (Exception)` 放宽为 `catch (Throwable)` 兜底防类初始化类 Error 再逃逸）
- **回复中途突然中断、视图缺段**：SSE 断流重连的「已收尾」收场分支此前只定格残缺占位，现改为按落库历史重建视图，补齐断流窗口内丢失的正文/工具段

## 0.3.2（未发行）

### 新增

- **LLM 设置模型管理交互升级（向智能体供应商对齐）**：
  - 后端新增 `POST /api/providers/{id}/models/probe` 探测端点，只从上游拉取模型列表返回前端，不直接隐式落库覆盖，与原先覆盖式 `POST /fetch` 区分开
  - 前端重构为结构化模型管理：点击「拉取上游模型」展开上游候选池，支持复选框自由勾选、搜索过滤、一键「全选」与「清空上游项」；已选模型以结构化 Chip 呈现（单项悬停删除 + 一键清空）；支持单行输入回车即加，同时保留批量文本编辑模式；改动后显式提示「未保存修改」与保存/放弃操作，杜绝隐式覆盖

### 修复

- **会话列表分组 Hover 布局抖动**：分组头行高固定 `h-[26px]`，编辑/删除按钮改为常驻 flex 占位并通过透明度平滑渐显（`opacity-0 group-hover:opacity-100`），彻底消除鼠标滑过分组时下方内容被顶下 2px 的跳动
- **顶级页面滚动条挤压内容导致横移**：新增跨浏览器 `.scrollbar-none` 工具类并应用于插件页、设置页、项目页等居中布局的整页滚动容器，避免内容变长撑出 8px 滚动条时导致内容向左位移跳动

### 优化

- **插件页面单卡一体化重构**：将插件元信息与该插件贡献的管理面板（`settings.plugins` 槽位）合并到同一张卡片内，分区呈现，消除原先两级独立卡片的割裂感；`PluginSlot` 支持按 `pluginId` 过滤挂载，插件禁用时管理面板随之卸载、卡片平滑收合；顶部「本地插件」降级为轻量分节头

## 0.3.0

### 新增

- **会话回合时间线（ZCode 式）**：思考/正文/工具调用按真实发生顺序交错渲染——工具行收紧为单行摘要（命令/路径/搜索词，完整参数与输出收进点击展开的 IN/OUT 区），思考段完成后折叠为"思考 · 持续 N 秒"，回合头部实时显示"已工作 X 分 Y 秒"，长回合结束后自动折叠为汇总条（点开回看全过程），最终汇报文本常驻。OpenCode 与 Claude 会话均生效；思考内容首次进入持久化，历史重载不再是"思考一坨、工具一堆、文本垫底"
- **会话列表功能增强（T-105）**：分组、右键菜单、拖拽排序、动效和呼吸指示器
- **引用提示卡重设计**：固定尺寸省略有度 + 底部标注引用来源
- **会话气泡渲染发送的图片**：粘贴/发送图片后用户气泡即时显示缩略图（点开为灯箱大图，Esc/点击关闭），不再只看到 `[图片 #n]` 纯文本。图片缩略图（最长边 ≤1024px，webp 等无法解码的原样保存）随发送写入工单克隆 `.gate/chat-images/`（追加进 `.git/info/exclude`，不污染 git status），消息文本内嵌 `[图片引用 #n]` 引用行——不落库、零 schema 变更，历史重载经新端点 `GET /api/tickets/{no}/chat-images/{file}`（带鉴权、文件名白名单 + 路径穿越防护）还原；发送瞬间以 data URL 乐观渲染，后端落盘后自动切换为可持久化路径
- **划选引用胶囊**：工作台任意页面左键划选文字即浮出动作菜单，「添加到对话框」把选中文本收进当前工单的输入框成为引用胶囊（单行预览 + hover/聚焦浮出原文 + × 移除，随下一条消息发送）；发送后用户消息气泡里引用片段仍还原为胶囊渲染。引用以 `⟦引用⟧…⟦/引用⟧` 内联标记随消息纯文本走全链路（Agent 读到原文，后端契约零改动）；胶囊与划选偏好按工单键 localStorage 持久化
- **划选菜单插件扩展点（`selection.menu`）**：SDK 新增 `ctx.registerSelectionAction({ id, label, icon?, when?, run })`——插件可向划选菜单追加「添加到 xxx」类动作（`run(api, text)` 提供 `addToComposer / insertText / toast`，`when({ text })` 按选中文本显隐）；内置「添加到对话框」为宿主一等能力不进注册表，插件动作追加其后；`quick-quotes` 插件内置「引用并追问」示例
- **插件系统（T-104 全功能）**：仓库内置 `@gate/plugin-sdk` 契约包与 `plugin-template` 模板；支持对话区快捷动作条（`composer.chips`）、插件整页（`nav.pages`）与管理挂件（`settings.plugins`）；内置 `quick-quotes` 快捷语录插件（支持增删改查、拖拽排序、状态绑定与三种动作模式）
- **插件设置提升为一级导航**：顶栏视图导航升级为「工作台 / 看板 / 项目 / 智能体 / 插件」，插件管理与扩展面板一键直达，移出全局设置二级菜单
- **发布提交携带重启轮次背景**：`round > 1` 时提交正文自动追加 `Restart-Reason` 段落，回溯上一轮未通过的评审结论（人工驳回 note / 引擎 findings 摘要）或发布未落地原因，告别单纯 "round 3" 盲区
- **数据布局分根（破坏性重构，未发行窗口内直接落地）**：小数据/配置（gate.toml、gate.db、令牌、审计、blobs）常驻系统盘每用户目录 `%APPDATA%\OpenWorktree\local-run`，重装/覆盖安装/在线热更新永不移动；工单克隆工作区与项目镜像 `auth-*.git` 这类大体积数据（node_modules 可达数 GB）统一放安装目录【同级】独立目录 `OpenWorktree-data\`——随安装盘、处于安装/热更新载荷之外，更新不覆盖、卸载仅显式询问是否删除。首次启动自动收敛旧布局：按 gate.db 活度选本体树，gate-home 搬入轻根、clones/auth 搬入重根，克隆 `.git/config` origin 与 DB 行内旧绝对路径同步重基，其余旧树改名归档保留
- **clone_path 存储根治**：`ticket`/`agent_session` 行内不再存死绝对路径——克隆根内路径统一相对化（如 `T-104`），读取时按当前克隆根解析回绝对路径；克隆根此后可整体搬家/换盘而无需改库
- **系统托盘（自绘面板）**：托盘图标常驻，主窗口点 ✕ 隐藏到托盘（进程与后端常驻，退出走托盘面板「退出」，点托盘图标或再次启动可唤回）；右键弹出应用自身设计风格的自绘面板（替代无法配色定制的原生菜单）——实时 Agent 运行数（面板展开期间 2s 刷新）、接入项目列表（绿色圆点 = 当前选中的工作台）、打开工作台/退出；点击项目直达对应工作台，Esc/失焦自动收起
- **数据目录随安装盘走**：默认落在安装目录 `data\`（工单克隆、数据库、令牌与配置），不再挤系统盘 AppData；卸载时询问保留（移至 `%APPDATA%\OpenWorktree`）或删除，重装自动搬回，旧版数据自动迁移
- **更新检查**：设置中心新增「应用设置」页——版本 / 更新检查 / GitHub 仓库；一键检测新版本，并区分「未公开先行者」与「网络不通」两种状态；发现新版本时自动展示本文件对应版本的更新内容
- **智能体会话级 1:1 归属**：新建会话进入草稿态（预配置 agent / 模型 / 推理），发送即建会话；失败回滚可原样重发；会话徽标（agent 名·日期）与顶部 agent 跟随会话
- **会话消息刻度导航（ChatRail）**：每条用户消息一根刻度，悬浮预览气泡、点击平滑跳转；任务/上下文圆环移入横条，弹层向下弹出
- **会话工具调用展示**：流式回合接入工具输出可展开，IN / OUT 分段可复制
- **npm SDK 包预设**：补全 ai-toolbox 同款五个——openai-compatible / openai / anthropic / google / xai

### 修复

- **回复中途突然中断、视图缺段（重连收场不重建视图）**：SSE 断流后若服务端已收到回合终点（上游 reader 与浏览器 SSE 是两条独立链路），重连收场分支只定格残缺的流式占位条目——断流窗口内丢失的正文/工具段浏览器永远收不到，而完整回复其实已落库（opencode 侧可见全量、OW 视图缺失）。现凡断流过的回合，收场后先终结占位条目再用落库历史重建视图；未断流的回合不动，避免重建抖动
- **opencode serve 进程死亡无迹可寻**：spawn 时 stdout/stderr 走 DISCARD 全部丢弃、也没有进程退出钩子——serve 崩溃（Node OOM/自身 bug/被杀）后没有任何尸检线索，"为什么断连"永远成谜；现改为追加到 `gate-home/serve-logs/serve-<port>.log` 保留遗言（跨重启累积），并注册 onExit 钩子在进程退出时按 exit code 落 INFO/WARN 日志
- **授权卡片要点工单才出现 / 授权应答失败（opencode permission reply failed）**：双根因——① `permission_asked` 只经 SSE 实时推送，断流/刷新期间到达的询问没有任何恢复机制，只能点工单 item 触发 REST 拉取；现 busy 轮询慢节拍对运行中会话主动补拉待决权限/提问，卡片自动挂载。② 权限/提问应答直接 POST 内存端口，serve 进程死亡后端口永久失效、应答永远失败（发消息路径有懒复活、应答路径没有）；现应答统一走自愈——探活确认 serve 已死后清理失效端口、按 send 同款懒复活拉起新 serve 并重试一次
- **会话事件流一次断流即弃疗**：SSE 连接级 error 直接关流清场，实时视图死亡、模型选择器变灰，只能等服务端下次推送间接恢复；现改为断流后查运行集决定重连或收场——会话仍在跑则补拉 pending 后 2s 重开事件流（实时视图/选择器自动恢复，断流窗口内的授权不丢），连续 5 次重连无事件才放弃并明确提示；后端显式错误帧仍按回合失败终局
- **新建工单基座落后 N 个提交**：建票克隆自权威镜像，但人在注册工作区的提交只有点「同步基座」才导入镜像——新工单一建出来就是旧基座，首点同步即报"快进 N 个提交"。现建票前先把工作区基分支 tip 导入镜像（对工作区只读、fail-open：导入失败降级为审计备注，绝不阻断建票），克隆起点即源仓库最新提交
- **编辑工单弹窗内容自动复原**：播种表单的 effect 依赖 ticket 对象引用，而后台轮询（busy 慢节拍补拉）与任务回刷都会整表重建 tickets 数组——弹窗开着时正在编辑的所有字段被复原回 store 旧值。现仅弹窗打开那一刻播种，此后不再跟随刷新重灌
- **发布报 "Cannot run program git（目录名称无效）error 267"**：分根迁移后 `ticket.clone_path` 在 DB 存的是克隆根内相对路径，`publish_intent` 读取端（JOIN 原始列）未按克隆根重基回绝对路径，发布进程拿相对路径当 CWD 必炸；读取端补与 ticket 仓库同规则的重基（构造注入 `clonesRoot`）
- **quick-quotes 插件激活报 "process is not defined"**：插件 vite 构建 `define` 补 `process.env.NODE_ENV → "production"`——依赖里的 Node 环境判定在浏览器宿主无此全局，激活即崩；构建期替换为字面量后死代码消除，产物不再引用 `process`
- **数据布局分根迁移的 auth 镜像静默漏搬（c6d4469 缺陷）**：壳侧 auth-*.git 搬迁失败既不落日志也不中止，仍写迁移标记 → 后端把 `project.auth_repo` 重基到空目录，后续项目建单全线 "auth repo does not exist"；现改为 auth 镜像先于 gate-home/clones 搬迁，任一失败即整体中止（不写 marker/gate.toml，下次启动重试）；后端侧增加兜底——重基前校验新路径镜像真实存在，缺失则拒绝改写并保留 marker；另修克隆 origin 改写对 git config 反斜杠转义（`\\`）不匹配导致的静默漏改
- **WS 1011（native）**：覆盖 Jetty ByteArray/ByteBufferMessageSink——MethodType 引用比较（if_acmpeq）在 GraalVM native-image 不保证同一性，Javalin 声明 (Session,byte[],int,int) 消息方法→OPEN 急切建 binary sink→InvalidSignatureException→每条连接 1011；新增 gate-ws-patch 模块（同 FQN 覆盖类逐字取自 11.0.20 源码仅改 equals），native-build 两平台 CP 前插保证先命中
- **冒烟脚本 ws_recv 裸 opcode 未掩 FIN 位**：hdr[0] 含 FIN 位（text=0x81/close=0x88），需 `&0x0F` 后再比较；WS 1011 修复后管线首次真正走到收帧路径才暴露此潜伏 bug
- **多行引用胶囊的原文提示被相邻消息截断/常驻不收**——三处样式根因
- **修复终端「创建后一直空白无响应」（native 版）**：GraalVM 镜像缺 WebSocket 栈运行时元数据——升级握手照常 101、但应用层帧（started/data/error）全部静默丢失；HTTP 冒烟测不到、TerminalApiTest 又属 slow 层被 CI 默认跳过，双重盲区放行。已把 io.javalin.** 与 Jetty websocket 栈全部类（383 个）补进 reflect-config，并给 native 冒烟新增裸 socket WS 断言（start 必须回 error 帧），回归将直接打红 CI。重编 native 后安装即修复
- **修复 WS 错误提示被 close 挤掉**：鉴权失败/目录白名单拒绝等路径先发 error 再立即 close，与未 flush 的帧竞态，客户端只见 1006 静默断开；改为 sendErrorThenClose（500ms 后关），实测错误帧先行、1000 干净关闭
- **修复终端打开后零反馈的黑洞体验**：连接建立但 8s 未收到 started 帧（如上述后端故障时），xterm 内写红色诊断提示指引用户重启应用，不再永远空白
- 修复会话流「卡在最底部滚不动、点刻度却跳到会话开头」：跳转/高亮共用的消息偏移测量基准把 scrollTop 加反（偏差随滚动距离翻倍，滚到底后 tops 全为负——点击刻度 `scrollTo(0)` 直跳顶部、刻度高亮停在同一根）；改为内容原点基准（可视顶 − scrollTop）并单调兜底，与当前滚动位置无关
- 修复吸底逻辑与用户上滚对抗：原「距底 <120px 即视为吸底」单向判定，从底部上滚的头几下始终落在阈值内，流式期间每次更新都平滑拽回底部，表现为滚轮无响应；改为方向感知吸附——向上滚动立即解除跟随，滚回贴底范围才恢复
- 修复流式回合期间整列表重渲染卡顿：AssistantMessage/SystemMessage/ThinkingBlock/ToolRow/Markdown 全部 memo 化（chat 补丁均为不可变更新，未变消息直接跳过 Markdown 重解析），流式更新不再阻塞滚动与点击响应；流式吸底跟随改瞬时贴底（smooth 动画被高频更新反复打断重启正是「滚不动」的观感来源之一）
- 修复光标落在消息刻度条上时滚轮失灵：刻度条悬于滚动容器外侧不透传滚轮，现手动转发给聊天滚动容器
- 修复草稿首条消息发送后「正在创建会话」遮罩不消失：遮罩原挂在整个流式回合结束，现消息被后端受理即撤（Agent 开始输出前），生成中状态无缝交给输入框
- 修复 opencode 孤儿 serve 端口占用：启动期按端口段扫描收割孤儿，分配期撞占用先自愈再跨步换端口，连环 stale-port 失败不再要求用户重发
- 修复工单创建 MCP 工具缺项目域约束：agent 域建单自动继承绑定工单的项目、跨项目拒绝，杜绝「派发给所有接入项目」的无主单
- 修复接入表单目录名自动填充与弹窗遮罩拖拽误关
- 修复聊天气泡内超长链接/路径不自动换行：气泡正文启用 `overflow-wrap:anywhere`，长 URL、文件路径与行内代码不再撑破气泡溢出

### 优化

- vite dev 代理默认后端端口 18080→19090（本机常驻后端端口）
- CI 冒烟诊断增强：断言失败时输出服务器 boot.log 尾部；native 运行时 WS 管线兜 Throwable 落日志
- **关闭全部终端支持确认与记住偏好**：多标签时点击「关闭全部」先弹确认（提示将结束全部进程），可勾选「不再提醒」（localStorage 持久化，设置后直接关）；单标签不弹确认
- **变更对比超大 diff 渲染根治**：文件列表改窗口化虚拟渲染，只挂载视口 ±缓冲内的文件块、滚出即卸载，DOM 规模与 diff 规模解耦（未忽略 node_modules 的数万文件/数十万行不再卡死）；单个文件内容按批展开（每批 2000 行）；审查跳转可跨未挂载文件定位目标行
- **测试分层**：重型端到端/故障注入/真实 CLI smoke 测试打 `@Tag("slow")`，默认 `mvn test` 只跑快速层（约 17 分钟 → 约 1 分钟）；`-Pintegration` 只跑重型层，`-Pfull` 全量（发版前）
- 设置中心改版：tab 更名「系统设置」，分区重排（Web 控制台 → 审查引擎 → 审查策略 → 智能体默认 → 提交身份 → 会话编排 → 高级设置），只读键不再展示，提交身份展示真实运行期默认行为
- 模型编辑器：档位选择框限制、变体一键模板、text 模态默认勾选
- 会话列表收展动效（空间重分配过渡，消除瞬移）


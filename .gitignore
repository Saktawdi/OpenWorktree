# Build output
target/

# Frontend local dependencies and Vite build output
gate-web-ui/node_modules/
gate-web-ui/dist/

# Gate 插件工程（plugin-template / plugins/*）：依赖与构建产物不入库
plugin-template/node_modules/
plugin-template/dist/
plugin-template/.vite-temp/
plugins/*/node_modules/
plugins/*/dist/
plugins/*/.vite-temp/

# Archival / test output — build artifacts dumps
doc/归档/
doc/local/
.archive/

# OhMyOpenCode agent state
.omo/
.gate/

# Playwright MCP QA artifacts
.playwright-mcp/

# IDE
.idea/
*.iml
.vscode/
.zcode/

# OS
Thumbs.db
.DS_Store
multica-ref/
gui-test-screenshots/
test.json

# Local development launcher & runtime home (gate.toml / auth.git / gate.db)
/start-local.bat
/local-run/
*.timestamp-*.mjs

# Reference projects cloned for study (not part of this repo)
docx/reference/
docx/

# Archify 运行时图表导出（本机可视化产物，不入库）
/local-git-gate.runtime.*
# Maven classpath 临时转储
gate-cli/gate-cp.txt

# release outputs (keep templates only)
release/OpenWorktree/lib/
release/OpenWorktree/ui/
release/OpenWorktree/config/
release/OpenWorktree/data/
release/OpenWorktree/__test_start.bat
release/*.zip

# native-image PoC build outputs (binary, staging; agent-config IS committed)
native-poc/ow-linux
native-poc/run/

# MCP QA screenshots (local tooling output, never tracked)
.openchamber/

# native-image PoC scaffolding stays local; CI needs only agent-config (now .github/native-image/)
native-poc/

# Tauri 桌面壳构建产物（sidecar 二进制按机器/CI 同步，不入库）
desktop/node_modules/
desktop/src-tauri/target/
desktop/src-tauri/gen/
desktop/src-tauri/binaries/*.exe
# dev-shim-ow 遗留的备份命名（本机可能留有真实 native 单文件，不追踪）
desktop/src-tauri/binaries/*.native-backup

# Maven CI-friendly 版本：flatten 插件生成（install/deploy 用），不进版本库
.flattened-pom.xml

# sidecar-jvm-shim static junction (points at dist), not tracked
gate-web-ui/.sidecar-classpath/

desktop/src-tauri/binaries/*.shim-backup
packages/*/node_modules/

# Java 相关
META-INF/
*.class

# 可执行文件
*.exe

# 临时目录
/io/
/smoke-run/

# 本机冒烟测试产物（esbuild 临时 bundle/temp 源文件，不入库）
gate-web-ui/.smoke*
